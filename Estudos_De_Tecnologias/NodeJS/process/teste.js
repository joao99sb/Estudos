const key = process.argv.indexOf('a') !== -1

const tabela = function(x,y){
    console.log('soma: '.concat(x+y));
    console.log('multiplicacao'.concat(x*y));
    console.log('subtracao: '.concat(x-y));

}



if(key){
   process.stdout.write(`esolha o primeiro numero \n` )
    const x = process.stdin.on()
}